/** Customer Types — Types for the customer system */

export interface CustomerRow {
  customerId: number;
  name: string;
  email: string;
  documentId: string;
  cellphone: string;
  createdAt: string;
}
